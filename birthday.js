// Wait for page to load document.addEventListener('DOMContentLoaded', function() { // Find the Categories dropdown const dropdown = document.querySelector('.dropdown'); if (!dropdown) return; // Safety check const categoriesBtn = dropdown.querySelector('.nav-btn'); const dropdownLinks = dropdown.querySelectorAll('.dropdown-content a'); // Check if any dropdown link is active let hasActiveLink = false; dropdownLinks.forEach(link => { if (link.classList.contains('active')) { hasActiveLink = true; } }); // If yes, add .active to Categories button for underline if (hasActiveLink) { categoriesBtn.classList.add('active'); } });